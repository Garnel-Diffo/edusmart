# EduSmart - Service IA Python (FastAPI) - Pipeline RAG
#
# Le wheel PyPI par défaut de torch embarque les binaires CUDA (~2 Go) inutiles
# pour ce service (inférence CPU uniquement, embeddings courts). L'index CPU
# officiel ci-dessous réduit l'empreinte à ~200 Mo, ce qui compte sur un plan
# Render gratuit/Starter à mémoire limitée :
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2

# Base de données (Neon Postgres + pgvector)
asyncpg==0.29.0
pgvector==0.3.4

# Embeddings (locaux, gratuits)
sentence-transformers==3.1.1
torch==2.4.1

# LLM (Groq - inférence rapide)
# >=1.5 requis pour le paramètre reasoning_effort (modèles GPT-OSS)
groq==1.5.0

# Extraction de texte des documents pédagogiques
pypdf==4.3.1
python-pptx==1.0.2
python-docx==1.1.2

# Tests & qualité de code
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
